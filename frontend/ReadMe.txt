For backend, navigate to project directory, open terminal/command prompt. Execute, `npm i` and then execute `node index.js`.

For frontend, navigate to project directory, open terminal/command prompt. Execute, `npm i` and then execute `npm start`.